 * include c headers directly in zig source
 * ??return expression
 * the null pointer maybe optimization
 * `*index` on for loops to assign to the iterator index
 * %%return expression
 * build static library and shared library at the same time
 * build example C program
 * testing (see primitives test)
 * install .h file and libraries
 * typedef with c header directives
 * char c interaction?
